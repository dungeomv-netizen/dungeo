# 🏪 유통기한 관리기 — 사용설명서

## 실행 방법
- **`실행.bat`** 더블클릭 → 검은 창이 뜨고 브라우저가 자동으로 열림
- 주소: `http://localhost:8800`
- **폰에서도** (같은 와이파이): 폰 브라우저에 `http://192.168.219.100:8800` 입력
  - (이 IP는 PC 상황따라 바뀔 수 있어요. 검은 창에 뜨는 `Running on http://192.168.x.x:8800` 주소를 폰에 치면 됩니다)
- 검은 창은 켜둬야 작동. 끄려면 창에서 Ctrl+C

## 쓰는 순서
1. **업로드** 탭 → "이번 사진 매장"은 보통 **자동(GPS)** 그대로
2. 제품별로 **사진 2~3장씩(바코드 / 날짜 / 신규면 앞면)** 여러 개 통째로 선택
3. **처리하기** → 결과가 뜸
   - ✅ **기입완료**: 시트에 날짜 자동 기입됨 (라이브 모드일 때)
   - 🔵 **미등록**: 시트에 없는 바코드 → `＋새로 등록하기` 누르면 새 줄 추가
   - 🟠 **확인필요**: 날짜가 애매하거나 매장을 못 정한 것 → 직접 골라 저장
4. **임박알림** 탭 → 4일 / 1주 / 2주 이내 + 이미 지난 것(최근 30일), 매장별로 확인

## 두 가지 초기 설정 (한 번만)

### 1) 매장 위치 학습 (자동 매장판별용)
- **설정** 탭 → 매장 고르고 → **그 매장에서 찍은 원본 사진 1장** 업로드 → 저장
- 두 매장 각각 해주면, 이후엔 사진 GPS로 매장을 자동 판별
- ※ 원본 사진만 GPS가 살아있음(카톡·캡처 거치면 지워짐). 안 되면 업로드 때 매장을 직접 골라도 됨

### 2) 구글시트 실제 기입 켜기 (라이브 모드)
지금은 **미리보기 모드**(결과만 보고 실제로는 안 씀)예요. 실제로 시트에 쓰려면:
1. [Google Cloud Console](https://console.cloud.google.com) 접속 → 프로젝트 새로 만들기
2. **"Google Sheets API"** 검색 → 사용 설정(Enable)
3. 왼쪽 **사용자 인증 정보(Credentials)** → 만들기 → **서비스 계정(Service Account)** → 이름 아무거나 → 완료
4. 만든 서비스 계정 클릭 → **키(Keys)** → 키 추가 → JSON → 다운로드
5. 받은 파일을 **`data\credentials.json`** 으로 이름 바꿔서 이 폴더 안 `data` 폴더에 넣기
6. 그 서비스 계정 **이메일**(...@....iam.gserviceaccount.com) 복사
7. 구글시트 열기 → **공유** → 그 이메일을 **편집자**로 추가 → 완료
8. `실행.bat` 다시 실행 → 상단 뱃지가 **"라이브 · 시트 기입"** 으로 바뀌면 끝

> 이 6~7단계가 헷갈리면 클로드에게 "구글 연결 같이 하자" 하면 화면 보며 단계별로 도와줌.

## 폴더 구성
- `실행.bat` — 실행
- `app.py` 등 `.py` — 프로그램 본체
- `data\stores.json` — 매장 GPS 기준(설정 탭에서 자동 생성)
- `data\credentials.json` — 구글 연결 키(직접 넣어야 함)
- `.env` — 설정값(포트, 알림 기준일 등)
- `uploads\`, `static\thumbs\` — 처리한 사진/썸네일

## 설정 바꾸기 (`.env`)
- `ALERT_DAYS=14,7,4` — 임박 알림 기준(일). 원하는 숫자로
- `EXPIRED_WINDOW_DAYS=30` — "이미 지남" 목록에 며칠 전까지 보여줄지
- `PORT=8800` — 주소 포트
