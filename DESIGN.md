# 매장 유통기한 관리기 — 설계 확정본

## 목적
작은 가게(매장 2곳)의 상품 유통기한을 사진만으로 관리.
사진 통째로 업로드 → 자동 인식 → 구글시트에 날짜 기입 + 임박 알림.

## 사용 흐름
1. 제품별로 사진 2~3장을 연속으로 촬영 (바코드 / 유통기한 날짜 / (신규면)제품 앞면)
2. 여러 제품 사진을 **통째로 업로드**
3. 프로그램이 자동 처리:
   - EXIF 촬영시각으로 사진을 **제품 단위로 자동 그룹핑**
   - 각 사진에서 바코드 디코딩 + 날짜(비전 OCR) + 제품명(앞면 있으면)
   - EXIF **GPS로 매장 자동 판별** (원본 사진만 GPS 살아있음)
4. 결과:
   - ✅ **등록 바코드**: 확인창 없이 해당 매장 시트에 날짜 바로 기입
   - 🆕 **미등록 바코드**: "미등록" 목록에 모음 → [새로 등록하기] → 제품명·바코드·날짜 자동 채워 새 줄 추가 (제품명 없으면 타이핑)
   - ⚠️ **날짜/매장 못 읽음**: "확인필요" 목록에만 표시 (팝업 아님)

## 임박 알림 (신규 요구)
- 유통기한 기준 남은 일수 계산 → 3개 목록으로 분리 표시
  - 🟡 2주 이내 (≤14일)
  - 🟠 1주 이내 (≤7일)
  - 🔴 4일 이내 (≤4일)
- 매장별로도 나눠서 볼 수 있게

## 규칙 / 예외
- "제조일 + 9개월" 처럼 시트에 이미 함수 걸린 항목 → 프로그램이 날짜 계산 안 함, 시트 규칙 존중
- 유통기한 2개 상품 → 날짜 2개(한 장에 둘 다 or 따로 2장) 모두 읽어 각 칸에 기입
- 확인창 없음 (사진 잘 찍는 전제). 애매한 것만 "확인필요" 목록으로 회수

## 형태
- 로컬 웹앱 (Python Flask) → PC 브라우저 + 같은 와이파이 폰 브라우저 둘 다 접속
- 아이폰 HEIC / 안드로이드 JPG 모두 지원, EXIF(GPS·시각) 파싱

## 기술 스택
- Flask (서버 + 업로드/결과 UI)
- zxing-cpp (바코드 디코딩)
- Gemini 비전 (날짜·제품명 OCR) — 기본 엔진
- pillow + pillow-heif + piexif (이미지/EXIF/HEIC)
- gspread + google-auth (구글시트 읽기·쓰기, 서비스계정)

## 남은 입력 (사용자 제공)
1. 구글시트 접근 (링크 공유 또는 xlsx) — 컬럼 구조 확인용
2. 매장 2곳 위치 (각 매장 원본 사진 1장 or 주소)
3. (기입용) 구글 서비스계정 연결 — 시트 구조 확인 후 단계별 안내
